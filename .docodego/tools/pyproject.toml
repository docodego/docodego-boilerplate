[project]
name = "ics-scorer"
version = "0.1.0"
description = "DoCoDeGo ICS Scorer — automated Intent Clarity Score for specification quality"
requires-python = ">=3.10"
license = "Apache-2.0"

[project.scripts]
ics-scorer = "ics_scorer.__main__:main"
